import styled from 'styled-components';

export const IconStyled = styled.div`
	.svg {
		&--journal {
			//fill: ${({ theme }) => theme.tiles.tile1.svgColor};
		}

		&--goals {
			//fill: ${({ theme }) => theme.tiles.tile2.svgColor};
		}
	}
`;
